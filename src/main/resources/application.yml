server:
  port: 8080
  servlet:
    context-path: /api
spring:
  properties:
    host-name: localhost
    github:
      token: ${GithubBearer}
    async:
      max-pool-size: 100
      core-pool-size: 20
    auto-upload:
      enabled: false
      period-mills: 720000 # 12 minutes
  application:
      name: good-first-issue-back
  datasource:
    password: SomeUselessPassword02
    url: jdbc:postgresql://${spring.properties.host-name}:5432/uj?currentSchema=gfi
    username: idk_user
  jpa:
    hibernate:
      ddl-auto: update

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.github.regyl.gfi.controller.dto